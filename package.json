{
  "name": "epub-renderer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "watch": "tsc && vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.7",
    "vite-plugin-singlefile": "0.7.1"
  },
  "dependencies": {
    "url-join": "^5.0.0"
  }
}
